<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-file=no">

        <!-- Bootstrap CSS -->
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css" integrity="sha384-GJzZqFGwb1QTTN6wy59ffF1BuGJpLSa9DkKMp0DgiMDm4iYMj70gZWKYbI706tWS" crossorigin="anonymous">
        <title>TinyApp</title>
    </head>

    <body style="font-size:20px; padding:50px;">
        <header>
            <% include ./partials/_header %>
        </header>
        <a href="/urls/new">Create New TinyURL</a> 
        <p>
        <h2>My URLs</h2>
        <% for (shortUrl in urls) { %>
            <table>
                <style type="text/css">
                table{
                    border-collapse: separate;
                    border-spacing: 10px;}
                    </style>
                    <tbody>
                        <th> 
                            <td><%= shortUrl %></b> -> <%= urls[shortUrl] %> </td>
                            <td><a href ="/urls/<%= shortUrl %>">Edit</a> </td>
                            <td><form method="POST" action="/urls/<%= shortUrl %>/delete" id="deleteform" style="display:inline">
                                <button type="submit" class="btn btn-danger btn-lg"><span class="glyphicon glyphicon-trash"></span> Delete</button>
                            </form></td></th></tbody>
                            <p>
                                <% }; %>
                            </p>
            </table>
    </body>
</html>